# Воспроизводимый ML-пайплайн: DVC и MLflow

Этот проект представляет собой воспроизводимый пайплайн машинного обучения, разработанный в рамках модуля "Основы воспроизводимого машинного обучения. Жизненный цикл MLOps".

Для обеспечения **воспроизводимости** и **отслеживания экспериментов** используются следующие ключевые инструменты:
* **DVC (Data Version Control)**: Для версионирования данных (`data/raw/iris.csv`) и определения ML-пайплайна (`dvc.yaml`).
* **MLflow**: Для логирования параметров, метрик и артефактов (обученной модели).

Для полного воспроизведения проекта и получения тех же метрик следуйте этим шагам.

### 1. Клонирование и Настройка Среды

1.  **Клонируйте репозиторий:**
    ```bash
    git clone <URL вашего репозитория>
    cd mlope_dvc_mlflow_project
    ```

2.  **Создайте и активируйте виртуальное окружение:**
    ```bash
    python -m venv .venv
    # Для Windows PowerShell:
    .\.venv\Scripts\Activate.ps1
    # Для Linux/macOS/Git Bash:
    # source .venv/bin/activate
    ```

3.  **Установите все зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

### 2. Запуск DVC и MLflow

1.  **Получите версионированные данные (из DVC-кеша):**
    ```bash
    dvc pull
    ```
    *Этот шаг восстановит `data/raw/iris.csv` и обеспечит наличие всех зависимостей данных.*

2.  **Запустите MLflow Tracking Server (в отдельном терминале):**
    ```bash
    mlflow ui --backend-store-uri sqlite:///mlflow.db
    ```
    *Оставьте этот терминал открытым. UI доступен по адресу `http://127.0.0.1:5000`.*

3.  **Воспроизведите пайплайн DVC:**
    ```bash
    dvc repro
    ```
    *DVC выполнит стадии `prepare` и `train`. Если параметры (`params.yaml`) не менялись, DVC пропустит стадии и подтвердит воспроизводимость.*

### 3. Проверка Результатов

* **DVC:** Проект должен завершиться сообщением `Data and pipelines are up to date.`, если не было изменений.
* **MLflow:** Откройте UI MLflow и убедитесь, что в эксперименте **"DVC\_MLflow\_Pipeline\_Iris"** зафиксированы:
    * **Параметры:** `random_state: 42`, `test_size: 0.2` и т.д.
    * **Метрики:** `accuracy` (Точность).
    * **Артефакт:** Обученная модель `model.pkl`.

---

## 🔬 Пайплайн и Модель

### Стадии DVC

| Название | Описание | Зависимости | Результат |
| :--- | :--- | :--- | :--- |
| `prepare` | Разделение сырых данных Iris на обучающую и тестовую выборки. | `src/prepare.py`, `params.yaml`, `data/raw/iris.csv` | `data/processed` |
| `train` | Обучение модели, вычисление метрики и логирование в MLflow. | `src/train.py`, `params.yaml`, `data/processed` | `model.pkl` |

### Модель

* **Тип модели:** Логистическая регрессия (`LogisticRegression`).
* **Параметры:** Определены в `params.yaml` (`solver: liblinear`, `max_iter: 1000`).
